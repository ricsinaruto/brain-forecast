# Configuration for preprocessing pipeline
# Paths should be adjusted per environment

# Root directory containing raw dataset
data_path: "/vol/data/datasets/MOUS/raw"
stage1_path: "/vol/data/datasets/MOUS/preprocessed_ss"
save_path: "/vol/data/datasets/MOUS/preprocessed_ss"
log_dir: "/vol/data/datasets/MOUS/preprocessed_ss"
delete_fif: false
maxwell: false
skip_done: false

# Number of dask workers used by osl-ephys
n_workers: 8

# Length of each saved chunk in seconds
chunk_seconds: 30
sfreq: 100
source_space: true

# Optional path to a custom OSL preprocessing config
osl_config: configs/preproc/osl_mous_small.yaml

# Parameters for additional stage-2 processing
preproc_config:
  normalize:
    method: "robust"
  clip:
    std_factor: 5
  mulaw_quantize:
    n_bits: 8
