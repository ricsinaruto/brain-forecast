num_channels: 272
vocab_size: 256
d_model: 256
layers: 4
trf_block: STConvBlock  # STGPTBlock, STGPTBlockParallel, STConvBlock

trf_args:
  attn_type: standard
  mlp_type: standard
  conv_kernel: [5, 5, 5]

  attn_args:
    d_model: 256
    nheads: 8
    dropout: 0.0
    rope: true

  mlp_args:
    d_model: 256
    # num_experts: 4
    # experts_per_token: 2