dataloader:
  batch_size: 8
  num_workers: 16
  persistent_workers: true
  pin_memory: true
  prefetch_factor: 4
datasplitter:
  dataset_class: ChunkDatasetReconstruction
  dataset_root: /vol/data/datasets/Omega/preprocessed_1to50hz_ss_cont
  example_seconds: 32
  overlap_seconds: 20
  test_ratio: 0.1
  val_ratio: 0.1
eval_runner:
  ckpt_path: /vol/data/trainings/omega/2sub/1to50hz_ss/flatgpt/brainomni/FlatGPTBlockCausal/logs/version_11/checkpoints/best-checkpoint-epoch00022.ckpt
  enabled: true
  epoch: 22
  generate:
    enabled: true
    kv_overlap: 32
    seconds: 32
    top_p: 0.8
  max_batches: 1000
  modal_app: ephys-gpt
  modal_function: runevals
  num_examples: 0
  step: 283
  use_modal: true
  version: 11
lightning:
  lr: 0.0001
  lr_scheduler:
    T_max: 100
    class_name: CosineAnnealingLR
    eta_min: 1.0e-05
  weight_decay: 0.1
loss_name: CrossEntropyWithCodes
model_config: configs/flatgpt/brainomni/model_block_qwen.yaml
model_name: FlatGPTEmbedsRVQ
resume_from: null
save_dir: /vol/data/trainings/omega/2sub/1to50hz_ss/flatgpt/brainomni/FlatGPTBlockCausal
trainer:
  accelerator: cuda
  check_val_every_n_epoch: 1
  checkpoint_cadence_epochs: 5
  default_root_dir: /vol/data/trainings/omega/2sub/1to50hz_ss/flatgpt/brainomni/FlatGPTBlockCausal
  gradient_clip_val: 1.0
  log_every_n_steps: 10
  max_epochs: 100
  precision: bf16-mixed